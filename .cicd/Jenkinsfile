pipeline {
    agent none

    stages {
        stage('Build Singularity Container') {

           steps {
              script {
              
              sh '''
                echo "Hello world!"
              '''
  
              }

           }
    }
  }
}
